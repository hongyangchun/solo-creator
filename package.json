{
  "name": "solo-creator",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "engine": "pnpm -F @solo-creator/content-os dev",
    "gui": "pnpm -F @solo-creator/gui dev",
    "test": "pnpm -F @solo-creator/content-os test",
    "type-check": "pnpm -F @solo-creator/content-os type-check && pnpm -F @solo-creator/gui type-check"
  }
}
